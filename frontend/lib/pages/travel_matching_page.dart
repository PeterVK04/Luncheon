// pages/travel_matching_page.dart
import 'package:flutter/material.dart';

class TravelMatchingPage extends StatelessWidget {
  const TravelMatchingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Travel Matching')),
      body: const Center(child: Text('Travel Matching Screen')),
    );
  }
}
