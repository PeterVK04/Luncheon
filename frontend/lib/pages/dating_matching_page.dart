// pages/dating_matching_page.dart
import 'package:flutter/material.dart';

class DatingMatchingPage extends StatelessWidget {
  const DatingMatchingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dating Matching')),
      body: const Center(child: Text('Dating Matching Screen')),
    );
  }
}
