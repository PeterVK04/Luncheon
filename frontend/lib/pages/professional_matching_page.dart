// pages/professional_matching_page.dart
import 'package:flutter/material.dart';

class ProfessionalMatchingPage extends StatelessWidget {
  const ProfessionalMatchingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Professional Matching')),
      body: const Center(child: Text('Professional Matching Screen')),
    );
  }
}
